import { PartialContext } from "../../src/PartialContext"

describe("PartialContext.ts", () => {
  let partialContext: PartialContext<string>

  beforeEach(() => {
    partialContext = new PartialContext()
  })
})
